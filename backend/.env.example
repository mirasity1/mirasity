# Backend Environment Variables
# Copie este arquivo para .env e configure suas credenciais

# SMTP Configuration
SMTP_HOST=your-smtp-host.com
SMTP_PORT=587
SMTP_USER=your-email@your-domain.com
SMTP_PASS=your-smtp-password

# Email Recipients
EMAIL_TO=your-email@your-domain.com

# SMTP Provider Configuration
# Set to 'mxrouting' for MXroute SMTP API (HTTP-based)
# Set to 'generic' for traditional SMTP providers
SMTP_PROVIDER=mxrouting

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend Configuration
FRONTEND_URL=https://your-domain.com

# Railway Variables (automatically set by Railway)
RAILWAY_STATIC_URL=
RAILWAY_PUBLIC_DOMAIN=

# === CONFIGURAÇÃO ===
# 
# Para MXroute:
# - SMTP_PROVIDER=mxrouting (usa HTTP API)
# - SMTP_HOST=your-mxroute-server.mxrouting.net
# - SMTP_USER=your-email@your-domain.com
# - SMTP_PASS=your-mxroute-password
# - EMAIL_TO=your-email@your-domain.com (destinatário dos formulários)
#
# Para outros provedores SMTP:
# - SMTP_PROVIDER=generic
# - Configure SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS
# - EMAIL_TO=your-email@your-domain.com (destinatário dos formulários)
#
# NOTAS:
# - EMAIL_TO define para onde vão os emails do formulário de contacto
# - Se EMAIL_TO não estiver definido, usa SMTP_USER como fallback
# - SMTP_USER é sempre usado como remetente (from)
#
# ROTAS DISPONÍVEIS:
# POST /api/send-email           - Enviar email
# GET  /api/test-smtp            - Testar configuração
# GET  /api/test-mxroute-api     - Testar MXroute API (se aplicável)
# GET  /health                   - Health check